git config --global user.name "yamadaannahs-cmd"
git config --global user.email "yamadaanna.hs@gmail.com"
# Salir de la carpeta levi (por si estás dentro)
cd .. 2>/dev/null
# Borrar carpeta levi si existe
rm -rf levi
# Clonar el repo original
git clone https://github.com/levi275/levi
cd levi
# Borrar historial git
rm -rf .git
# Inicializar nuevo repositorio
git init
# Conectar con TU repositorio
git remote add origin https://github.com/yamadaannahs-cmd/Anna-Yamada
# Agregar todos los archivos
git add .
# Hacer commit
git commit -m "Subiendo archivos base"
# Forzar rama main
git branch -M main
# Subir a tu GitHub
git push -u origin main
cd ..
ls
# Salir de la carpeta levi (por si estás dentro)
cd .. 2>/dev/null
# Borrar carpeta levi si existe
rm -rf levi
# Clonar el repo original
git clone https://github.com/levi275/levi
cd levi
# Borrar historial git
rm -rf .git
# Inicializar nuevo repositorio
git init
# Conectar con TU repositorio
git remote add origin https://github.com/yamadaannahs-cmd/Anna-Yamada
# Agregar todos los archivos
git add .
# Hacer commit
git commit -m "Subiendo archivos base"
# Forzar rama main
git branch -M main
# Subir a tu GitHub
git push -u origin main
rm -rf levi
ls
rm -rf levi
# Salir de la carpeta levi (por si estás dentro)
cd .. 2>/dev/null
# Borrar carpeta levi si existe
rm -rf levi
# Clonar el repo original
git clone https://github.com/levi275/levi
cd levi
# Borrar historial git
rm -rf .git
# Inicializar nuevo repositorio
git init
# Conectar con TU repositorio
git remote add origin https://github.com/yamadaannahs-cmd/Anna-Yamada
# Agregar todos los archivos
git add .
# Hacer commit
git commit -m "Subiendo archivos base"
# Forzar rama main
git branch -M main
# Subir a tu GitHub
git push -u origin main
# Clonar el repo original
git clone https://github.com/levi275/levi
cd levi
# Borrar historial git
rm -rf .git
# Inicializar nuevo repositorio
git init
# Conectar con TU repositorio
git remote add origin https://github.com/yamadaannahs-cmd/Anna-Yamada
# Agregar todos los archivos
git add .
# Hacer commit
git commit -m "Subiendo archivos base"
# Forzar rama main
git branch -M main
# Subir a tu GitHub
git push -u origin main
